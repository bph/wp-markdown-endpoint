<?php
/**
 * Uninstall handler for WP Markdown Endpoint.
 *
 * This plugin does not store any data in the database,
 * so there is nothing to clean up on uninstall.
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	exit;
}
